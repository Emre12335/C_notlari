/* atom kısayolları */
/*
ctrl + shift + 'd' : line ı klonlama
ctrl + 'l' : line ın tamamını seçme
(line ın tamamını seçtikten sonra ctrl basılıyken yön tuşlarıyla line ın konumunu değiştirebilirsin)
ctrl + z : geri alma
ctrl + x :kesme
ctrl + c : kopyalama
ctrl + v : yapıştırma
ctrl + n : yeni dosya açma
ctrl + s : dosyayı kaydetme
tab: kaydırma
shift + tab : kaydırdığını geri alma
alt + tab : açık pencereleri görme
(alt tab yaptıktan sonra alt a basılı tutup tab a basarak seçili dosyayı değiştirebilirsin.)*/
#include <stdio.h>
int main(int argc, char const *argv[]) {
  return 0;
}
